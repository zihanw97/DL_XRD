# torch is deliberately not pinned here: the correct build depends on your
# CUDA version (or CPU-only). Install it separately first following
# https://pytorch.org/get-started/locally/ -- this repo was developed
# against torch==2.6.0 (CUDA 12.4 build).
numpy==1.26.4
scikit-learn==1.5.1
scikit-image==0.24.0
matplotlib==3.9.2
Pillow==10.4.0
tifffile==2023.4.12
openTSNE==1.0.4
